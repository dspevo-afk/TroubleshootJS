# Final visual/serviceability qualification

PASS for the authorized visual pass. P05 remains unstarted.

The current completion commit is based on f41e80f. See qualification-summary.json,
player-acceptance.json and root-review.md. The execution input set contains1154
frozen files; final verification found zero input drift. Build33 compiles all
five GWT permutations. Q15-33h and its forced negative run on that output.

## Candidate boundaries

The only non-output32-to33 changes are Q15SupportChecks.java and its independent
native test. Full Alpha38, service/RENDER0/A07/A08/E03/A10/Task41/Task49 and
architecture/layout/meter/U01/stored-energy results are explicitly reused from32.
The full39-suite native31 result is reused through its recorded32 input audit,
with fresh architecture fixture32 and Q15 fixture33b. This is not a claim that
every suite ran anew on33. collected.json maps proofs to actual origins/hashes;
reuse-audit.json and the two frozen input manifests support the reuse.

strict33.log records maintained Alpha38+38 malformed controls, Q15all11+22
malformed controls, A10, E03/A08/A07/Task41/Task49 and service/render readers.
Task43P's raw runtime remains OBSERVED; the actual maintained runtime oracle
accepts it with zero blockers and rejects a mismatched run. Its strict result
is separate. No Windows CLI-wrapper qualification is inferred.

## Fresh normal-player interaction

Real CDP mouse/key input in the actual headless compiled browser: normal menu
New board -> RB15seed3 progress -> ticket -> workbench; meter drag, bottom
copper and diagonal pan limit; exact Parallel0 -> OL/680 Ohm measurement ->
healthy-part removal/failed retest/restoration -> shop330 acquisition -> drag
installation -> powered customer retest PASS. Results were also inspected at
1024x768. Public metadata scan found no solution field or label. These are
operator-driven browser trials, not human difficulty/population measurements.

Five inspected images: 01-menu, 02-control-random-progress, 03-control-top,
05-control-pan-limit, 15-repair-passed. Other raw screenshots remain in the
task-owned OS-temp evidence folder. The progress bar reports real stages and
completed work; the observed larger board still took about36 seconds.

Retained failures and their dispositions are explicit. Exact cleanup succeeded
for final browser33h, prior33 and32r. An earlier exited browser32's unverifiable
profile/claim is preserved. No unrelated coding processes were terminated.

## Limits

HARD/PSYCHOTIC, arbitrary-seed admission, human difficulty calibration, complete
SVG product rendering and P05 are not delivered by this task. GWT24 pilot
wrong-value screenshots are not relabeled final33. Native audio contracts
prove real relay edges; this pass does not claim subjective speaker listening.

SHA256SUMS.json inventories this directory excluding itself.

### Publication-only normalization

The staged whitespace check found three otherwise blank lines in the developer
LU oracle and Windows line endings in copied logs. Only that whitespace was
removed. A fresh all-five-permutation GWT34 build passed and produced byte-for-byte
identical compiled output to tested GWT33. `publication34-format-audit.json` and
`candidate34-inputs.json` record the sole source-byte change; no executable token
or line number changed. Browser/player evidence remains correctly labeled33.
Published text uses LF; collected.json keeps both raw and published report hashes.
