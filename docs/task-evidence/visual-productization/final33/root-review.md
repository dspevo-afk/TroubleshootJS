# Integrated root review

Scope: actual accumulated visual/serviceability working tree plus the final Q15
proof repair, based on the existing f41e80f candidate. No independent agent was used.

Reviewed the owner/transaction paths for service slots, connector docking,
canonical bindings and failure compensation; fault-blind session/progress state;
physical scene, target projection, lifted tips and viewport camera; the actual
CircuitJS hot-loop diff; UI modal leases/instruments/audio; and preview/CDP
identity handling. The native and compiled negative protocols remain required.

The only newly found defect repaired at final33 was Q15 proof target selection.
It now follows declared command/load harnesses rather than graph iteration order.
The native negative fixtures compare independent actual family targets and
exercise reordered, absent and ambiguous switches. Models and numeric thresholds
were not relaxed to pass the check.

Final33 source reuse is deliberately narrow: only Q15SupportChecks and its
native test differ from32; their build/native/browser checks are fresh. Other
compiled32 and native31 results retain actual candidate provenance in reuse-audit.
The Windows CLI wrapper, arbitrary seeds, broad browser support, a complete
SVG renderer, and human difficulty calibration are not newly qualified.
