# A07 qualified execution and model-probe evidence

A07 is complete as the bounded serialized execution/observation foundation. A08
remains unstarted. Exact qualified code/test hashes, compiled digests and review
outcomes are in `candidate-provenance.json`. CircuitJS remains electrical truth.

## Final-source qualification

The maintained native runner exited 0: 17 Java suites, 38 A07 pure assertions,
140 A06 assertions, independent seed/value/role oracles, and 259 report assertions.
Compilation scratch cleanup passed. The JDK8 production OBF build compiled all
five GWT permutations and exited 0. The retained-launch identity canary covered
both fast natural exit and bounded child cleanup.

All nine actual compiled routes matched their expected outcomes through the
maintained extraction and strict readers. A07 recorded 38 pure, 53 runtime and
59 model assertions: 15 runtime cases, 21 model rows, 16 scale rows and six
execution/cancellation latency cases. A06 coverage includes positive/forced/
debug-off behavior, reconstruction, current repair and RC stored-energy discharge.
Normal positive and debug-off routes produced no browser errors. The A06 forced
negative retains its one deliberate AssertionError after the exact forced marker.

The actual A06 CLI attempt failed process-ownership preflight before application
execution. It is NOT certified. The authorized equivalent used a nonce-identified
task preview, fresh isolated Chromium contexts, and the exact maintained extraction
and strict readers. This proves compiled behavior, not the failed CLI wrapper.
`compiled-validation.json` records the reader outcomes; `compiled-reports.json.gz`
retains all nine raw reports.

Final-build player smoke used real Win32 mouse/keyboard input in an exact owned
browser: repeated loaded DC and isolated resistance, powered-OHM rejection,
removal of the measured bad R1, installation of the normal catalog 1 kOhm part,
simulated board power restoration and ordinary customer retest. The visible
result was "Repair verified. Indicator operating normally", with zero normal-play
browser errors. Four screenshots and the OS action receipt retain this sequence.

## Ownership and actual browser yielding

Compiled cases cover competing and same-token recursive execution, source/time
invalidation, nonfinite full matrix values including reduced constant rows,
singularity, nonconvergence, event ordering/finite feedback, successor graphs,
injected cleanup failure and obsolete callbacks. Private proofs never advance
protected original elements or restore over a successor. Cancellation is tested
before/during execution and at publication.

GWT incremental commands can be batched within one browser turn. The final
executor therefore owns an explicit Timer continuation and cancels that timer on
termination. Multiple independent timer heartbeats between slices and a real
queued cancellation prove actual browser yielding.

Measured success: 200 accepted steps in 13 timer slices, 52 ms wall time,
5 ms maximum measured inter-slice wait. All cancellation rows are retained.
Zero measured milliseconds reflect clock resolution, not zero physical latency.
Work/time checks occur at solver boundaries, not by preempting an OS or matrix call.

## Model and scale observations

Relay energization/release, linear-transformer loaded ratios, nonlinear diode
forward/reverse behavior and actual switched-buck startup/load-step probes passed
at two explicit time steps each. Three numerical-reference placements preserve
the divider differential of 2.5 V and 2.5 mA without claiming physical earth.
Declared assumptions, independent physical bounds and all readings are retained
in `a07-report.json`. Mechanical bounce, transformer saturation/mains fidelity,
converter closed-loop/thermal behavior and playable provider families are not
qualified by these small solver probes.

Scale samples are two fresh-context replicates per size and seed 0/1. They are
not reusable-context cold/warm benchmarks or physical/playable Q60/Q100 boards.
Every row has one analysis and 0.00001 s simulated duration.

| Resistors | Solver elements | Matrix full/reduced | Accepted/trials | Factorizations/solves | Wall ms range | Matrix double-slot bytes |
| --- | --- | --- | --- | --- | --- | --- |
| 20 | 22 | 23/21 | 2/2 | 1/2 | 0.00 to 1.00 | 7760 |
| 40 | 42 | 43/41 | 2/2 | 1/2 | 0.00 to 1.00 | 28240 |
| 60 | 62 | 63/61 | 2/2 | 1/2 | 0.00 to 1.00 | 61520 |
| 100 | 102 | 103/101 | 2/2 | 1/2 | 2.00 to 2.00 | 166480 |

The bytes column counts matrix double-storage slots, not browser/process heap.
These tiny linear workloads do not predict arbitrary large nonlinear boards.
Accepted-boundary scheduling does not implement sub-step event localization.

## Review, provenance and resources

One independent read-only gpt-reserve MAX session performed the initial review
and two bounded delta reviews. The initial runner-summary variable and misleading
cold/warm labels were corrected. Both deltas, including explicit timer-turn
yielding/cancellation, returned PASS. Review files distinguish independent source
review from root-executed tests; no independent browser execution is invented.

`build.log`, `native.log`, `build-process-canary.log`, candidate fingerprints,
compressed full reports and screenshots retain reproducible evidence. The native
receipt is framed text, so `native-receipt.txt.gz` preserves its original framing
rather than pretending it is plain JSON. The current maintained entrypoints remain
`verify-current-contracts.ps1`, production `build.ps1`, and
`verify-a03-browser.ps1 -Gate A07`, with the explicit CLI limitation above.

All task-owned test/reviewer workers, browsers and preview listener are closed.
Only the root publication terminal remains until commit/push/email finish; see
`cleanup.json`. Unrelated user processes and the pre-existing Python cache were
not touched. A08 is unstarted.
