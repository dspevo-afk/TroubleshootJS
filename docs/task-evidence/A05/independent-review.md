# A05 independent review and adjudication

Source and qualification disposition: PASS after the recorded corrections.
Final documentation delta: PASS; actual final commit/push/email outcomes are recorded separately.

## Review chronology

The original independent Reserve integrated review was BLOCKER FOUND, not an
initial PASS. It identified the A04 controlled verifier asking the load owner
for a physical RETURN pad even though that return was logical-only. The root
and a separately assigned verifier author repaired the fixture and associated
setup/ownership defects. These were reviewed as corrections, not suppressed.

The recovered diagnostic investigation traced earlier graph-membership and
settlement failures but did not itself resolve them or run new gates. The
mutation-settlement review statically accepted settlement before snapshots,
restoration and subsequent invariant checks; it explicitly left then-failing
compiled qualification to the root. Subsequent actual compiled runs closed
that qualification. Its historical failure report is not relabeled PASS.

The final A04 delta review accepted independently derived six-node physical
relationships, all 15 pairwise short checks, and element-shape/association
negatives. Construction net-partition, exact participant/receipt ownership and
abort invariants remained enforced. The final native and compiled runs are
separate execution evidence, not work claimed by the static reviewers.

## DeepSeek V4 Flash retry

The latest user instruction explicitly replaced new Reserve workers with
DeepSeek V4 Flash. Two fresh read-only leaf reviews used model
`deepseek-v4-flash`, provider `deepseek`, and MAX effort; all three values were
observed in runtime metadata. Neither reviewer authored implementation or test
oracles. No nested delegation, global configuration edits or speed override
was used. Earlier Reserve author/reviewer work retains its actual attribution.

`source-final`: PASS for the inspected A05 source contract, no current blocker.
It inspected the diff, role selection, NMOS/NPN primitive correspondence,
repeated-instance joins/reservations, physical provenance, support behavior,
current reconstruction, diagnostics, mutation settlement and relevant tests.
It performed no build, native run or browser playthrough.

`evidence-final`: PASS for the recovered source/qualification evidence, with
mandatory accounting corrections before publication. It independently
recomputed execution provenance and all 915 source-file hashes, checked the
six raw route reports, exact current parity, all 16 admitted fault-owner repair
cases across four compositions, support negatives/restoration, and player
screenshot hashes/timestamps against the operator journal. It did not rerun
the production build or browser/player flow and could not visually inspect the
images in its runtime. The retry root visually inspected the recorded repaired
NMOS/NPN and wrong-repair screenshots.

## Reconciled evidence

The maintained execution-provenance source-tree digest is
`48541648277a27ee8974f9d9093a2c1281b99b8be85d7ea97a6060f84d698f2d`.
This is the digest over the current `src/` scope, not an unspecified Java-only
hash. The full source/script/web/execution tuple is in dependency-audit.json.
All 1,320 consumed execution files match the accepted final tuple.

The accepted actual production build took 58.307 seconds to compile and
1.306 seconds to link five permutations. Native physical assertions total
5,270. The ordinary wrong-repair screenshot documents 100 kohm; the separate
compiled negative uses 10 Mohm. These replace stale summaries.

The retry root freshly reran the maintained native runner: 15 Java suites,
22 seed / 9 value / 6 role vectors and 195 report assertions passed with exit 0
and scratch cleanup. It rechecked all six captured compiled reports against
fresh native receipts, the current maintained reader and freshly recomputed
execution hashes. Identity, three manifests, seed/role and binary64 value
parity passed. This was not a new browser run; source and web are unchanged.

Player actions are recovered operator-recorded evidence, not independently
captured OS input telemetry. The source reuse boundary contains only later
developer-verifier changes. Real screenshots and action timestamps support
the recorded normal-player states; no independent physical-input certification
is claimed. The strict isolated CLI wrapper remains separately uncertified.

## Nonblocking findings

- Unreachable singleton convenience helpers in the contribution and Task41
  code can be removed when their owning boundary is next edited. Current
  generation does not invoke them; no retired generator is enabled.
- Channel-operation admission uses a bounded spelling check, while the current
  provider emits the exact operations that compiled proof executes. A future
  provider needs operation-catalog conformance before consumption.
- The pre-existing tests/contracts/__pycache__ files are excluded from staging.

These do not add a general cleanup campaign or expand A06/A09. The implemented
content remains the bounded two-channel role proof, not whole-game pluggability
or larger-board qualification. Root owns final adjudication and publication.

## Final documentation delta

The third DeepSeek V4 Flash MAX read-only review returned STATUS: PASS after
checking the corrected four-file hashes, the source inventory, raw accepted
receipts and the fresh retry checks. It did not rerun production or player
execution. The failed prior closure invocation remains failed.

Raw closure-result SHA-256: `c7d04324176ee4367ee93cfdc0bd711a5f60d3c7ed407dcf3c81351c3e95b246`.
