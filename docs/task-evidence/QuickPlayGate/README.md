# Post-P09 Procedural Quick Play admission gate

Status: COMPLETE - QUALIFIED for RB15_CONTROL / EASY.
Base: `83d3eb39eb3669b756a78588d95bdf155cecb172`.

## Delivered boundary

Normal New board and Quick Play retain all 64 bits of signed launch entropy for
RB15_CONTROL/EASY. The existing serial GenerationJob tries at most four distinct
ordinal candidates, beginning with that exact seed. The remaining candidates use
an odd modular stride. They share the existing 90-second, 640-work-unit and
five-second active-unit limits. No retry renews a budget or races another candidate.
Only typed expected incompatibility or routing/envelope rejection permits retry,
and only after exact successful restoration of the previous live owner.
Cancellation, stale ownership, programming errors, failed cleanup and deadlines
terminate the launch. No unsuccessful candidate receives a ticket or replay.

The admitted board still traverses actual healthy/faulty CircuitJS behavior,
physical geometry and copper correspondence, all three diagnostic hypotheses,
real service/repair checks and atomic publication. Nothing replaces those proofs
with a seed list, package count, routing score or cached passing result.

The session retains the accepted seed, not the initial entropy. Current replay
`tsj-alpha/2` reconstructs that one exact candidate without retries; obsolete
interpretations reject. Request version3, layout12 and dependency epoch10 include
the admission policy. P09's physical bounds are unchanged: its envelope itself
never certifies a procedural population. QuickPlayAdmission owns this separate
family/profile qualification. Fixed-layout reference families remain curated.
New package/pose/domain, two-layer/via/link and Q30/Q60/Q100 support are not claimed.

## Population and layout qualification

PLAN and CRITERIA were frozen before holdout execution: independently salted
SplitMix64 populations of 24 development and 48 holdout seeds. Every seed remains
in the report, including rejected boards and the development timeout. All accepted
native layouts regenerate from a fresh plan/board, and actual compiled admission
must match the native design, dimensions and every component origin exactly.

Development: 21/24 structural passes, 20/24 actual admissions, three expected
rejections and one timeout. Holdout: 42/48 structural passes and 42/48 actual
admissions, six expected rejections, no timeout or unexpected runtime failure.
These are observations on a predeclared qualification sample, not a universal
success guarantee or an estimate for every possible circuit/family.

Seeded ordering changes whole functional regions from the first placement attempt,
rather than relying on value changes, tiny coordinate jitter or only changing
fallback layouts. Connectivity, sensible functional grouping and package access
remain validated. The admitted holdout has 29 macro groups, six aspect bands and
a largest near-clone group of four out of42. The predeclared acceptance required
at least24 admissions,12 groups,3 bands and no group above25%.

The independent reader measures normalized component displacement with exact
integer cross-products and ignores whole-board translation. Reused names, changed
values/faults and raw geometry hashes alone do not count as variety. It rejects22
malformed controls, including a deliberately cloned admitted population. Two
isolated browser owners evaluated independent cases concurrently; each launch
remained serial. Timings are not a single-browser performance benchmark.

## Final validation and preserved work

The clean publication passes all 51 maintained native suites plus independent
oracles, the actual five-permutation JDK8/GWT build, full Q15 eleven-board repair
qualification, RC, physical layout, P07 prototype/probe checks and A10 lifecycle
qualification with its strict reader. Forced-negative routes retain their expected
failure markers. The preserved checkout separately passes 12 focused native suites,
its own five-permutation build, Q15/RC/layout, real retry/replay/cancellation and
normal player inputs. All nine current family entries pass on both variants.

The fresh complete compiled canaries prove exact rejection, two-candidate success,
two separate regenerations of the accepted seed, and cancellation during healthy
and diagnostic preparation. The strict population reader and both Q15 readers run
against actual reports and reject their malformed controls. Node checks pass112 UI,
358 bench-meter and4,103 signed-entropy assertions/vectors. Only the obsolete48-bit
UI expectation changed after the holdout, not production code or criteria.

Actual browser input includes untouched random New board, a documented controlled
entropy fixture with real DOM clicks for reproducible retry, failed unrepaired
customer retest, power isolation, exact rejection and cancellation retaining the
old board, rejection of old replay epoch, accepted exact replay, face/pan/zoom and
normal debug-off privacy. Correct-repair coverage comes from actual compiled
admission/Q15, not a claim of another human repair session.

The evidence distinguishes clean population results from the checkout containing
unrelated packing/FPS/tray work. All97 original files survive:93 byte-identical,
plus four shared files with exact inverse-merge proof. All selected owned browser
processes and listener threads closed. Recovery scratch and uncertain older
resources were left alone. No FPS, universal browser, modest-host, every-seed or
large-board certification is implied. U02 is next and was not started.
