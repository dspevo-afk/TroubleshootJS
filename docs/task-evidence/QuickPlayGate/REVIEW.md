# Implementing-root review

The root reviewed the integrated source and current callers. No separate human
or agent review is claimed. Independent executable expectations include signed
seed conversion, exact population derivation, physical correspondence, malformed
receipt controls, native scheduler failures and actual compiled diagnostics.

The normal UI and normal URL entry use PlayerLaunchRequest with the declared
family/profile. Developer-only legacy entry points remain separate. Search and
exact requests cannot be confused: a search cannot be resolved directly or
advertised as a replay, and the accepted identity must belong to its ordinal
candidate set. The generic manifest sort remains unchanged; bounded ordinal
prefixes select order independently of signed-seed lexical order or completion time.

Shared launch budgets are not reset. Candidate diagnostic counters reset while
aggregate work/timings remain monotonic. Expected rejection restores the original
owner before starting the next candidate. Non-retryable failure or incomplete
cleanup never continues. Stage-local proof/program counts retain all hypotheses.
The native negative services are explicit test doubles, not claims of browser
cleanup proof; real canaries separately exercise retry and routing/proof cancellation.

The normal player snapshot contains public progress and accepted replay only.
Exact replay does not retry, and the current epoch rejects earlier interpretation.
Crypto byte-to-decimal conversion does not pass a long through a JavaScript Number;
BigInt appears only in the independent Node test. No cryptographic guarantee is
made for the fallback or legacy Java Random source. Catalog reference families
remain curated rather than receiving unsupported population/diversity labels.

Regional variation changes a complete functional grouping, not the electrical
netlist, package geometry or clearance/access floor. All physical and live
correspondence checks still apply. P09's own bounds are not a population certificate.

The final exact-mode browser canaries retain a complete single-attempt ledger.
UI progress polling is observational: a fast rejection can finish before the
first poll. The corrected observer requires the actual ERROR/identity and retained
isolated owner, and rejects any observed search candidate count. It does not infer
retry from an empty sample. Earlier selector, report-attribute and sampling failures
remain separate failures; only the repaired harness results qualify their gates.

Four final screenshots were inspected: visible Candidate2 progress with aggregate
elapsed/work counts, integrated front board, replayed copper after navigation,
and the clean front board. Functional grouping, labels, accessible pads and the
separate bench meter are legible; the bottom view has no ghost top bodies. Partial
meter clipping while zoomed is the preserved bench-world behavior, not a new
screen-lock regression. Screenshots do not independently certify electrical
mapping, FPS or every possible seed's readability.

No blocking source defect remained after the final gate/readback review. Clean
and integrated consumed source hashes are separately recorded. Only the clean
candidate blobs are published for shared files; the original visual/FPS/packing/
tray changes remain unstaged, with93 exact originals and4 exact inverse merges.
