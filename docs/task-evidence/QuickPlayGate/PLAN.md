# Post-P09 Quick Play admission gate

Base: 83d3eb39eb3669b756a78588d95bdf155cecb172.
Scope: bounded arbitrary-candidate-seed admission for RB15_CONTROL/EASY, the
existing procedural family. Fixed-layout reference families remain explicitly
curated until independently qualified for meaningful layout diversity. No family
is called broadly procedural merely because a seed reaches its generator.

Use the existing serial GenerationJob and live CircuitJS admission path. At most
four unique signed-long candidates, deterministic ordinal order, shared unchanged
90-second/640-unit/five-second-unit limits. Typed candidate rejection alone may
retry after exact owner restoration; bugs, cancellation, stale ownership, failed
cleanup and global deadlines terminate. Exact replay never retries another seed.
Store the accepted candidate seed/profile/current epoch, not the launch entropy.
Full unsigned entropy is transported as canonical signed decimal text.

Predeclared population: 24 development + 48 untouched holdout seeds, separately
salted SplitMix64 sequences from 2026091601 and 2026091602. Freeze numerical P09
limits. Development measurements may inform algorithm changes; holdout is not run
until code/criteria freeze. Keep every rejection and failure by stage. Replays
must match discrete design/geometry. Qualify >=50% of holdout structurally and
require >=12 non-near-clone layouts, >=3 aspect bands, and no macro cluster over
25% of accepted holdout. Cluster ignores translation and uses >=8% normalized
part displacement; value/fault/name/trace-fingerprint-only differences do not count.
Real compiled admission covers all 72 candidates through the production engine,
plus regression eleven, forced rejection/retry, cancellation, owner cleanup,
wrong/correct repair/strict reports, exact accepted replay and normal mouse UI.
