# Integrated candidate review

Status: Shop/compatible-installation, launcher and exact meter endpoint source
review PASS. All final technical gates pass on execution6c330f50; final normal
entry observations and publication are recorded separately in the gate ledger.
The review below qualified the prior frozen candidate and did not catch the
cross-target purchasing restriction found by the user's MEDIUM trial.
Base: `ec10aa8b849149c33ae00153873c94c6aa6c9ca6`.

Current correction findings: generic grouping originally collapsed different
NPN resistor geometries. Root's real native NPN0 construction reproduced
RLOAD/RB spacing differences; recipe-plus-geometry grouping now preserves both
options, with76 focused native assertions. The public controller maps an
ephemeral specification handle to the real recipe/geometry source. These handles
are scoped to the immutable board/session and are not replay or persistence IDs.
A read-only Luna catalog review found current duplicate recipes identical; future
catalog implementations must preserve full specification equivalence, not merely
reuse a label/ID. Its concurrent UI finding referred to the pre-enumeration caller;
the final compatible-target changes now have root review, actual compiled
composed0/3 canaries and visible repeated-transfer/customer-retest evidence.

Root reviewed the final source inventory versus target mount split, exact typed
slot/geometry admission, preserved original-part restriction, actual-current
stress ownership and canonical empty-slot restoration. The latter retains
construction-time binding/endpoint/attachment references, not a second current
graph. Every removal remains inside the existing scope and compensation ledger.
The seven-provider failure matrix expands from223 to230 rows; each composed
cross-target fixture expands from22 to26, including removal after a cross-mount.
No original row or invariant was removed. Both composed0/3 fixtures now pass;
the same purchased330Ohm part also completed visible A→B→A moves and customer
retest. Native33 and actual GWT five permutations pass. Alpha38 then passed1245
assertions/230 mutation checks with owner restoration. The later continuity fix
also passes fresh Alpha38, full Q15/66 copper readings, native/build, affected
compiled regressions and strict negative readers on the final candidate.

The user's RB15 continuity finding was reproduced with real controls: J1.1 red
to F1.1 black read OL, while reversal read0 Ohm. Interactive drag snapped only
the red measurement endpoint to the editor grid. All three meter stimuli now
attach to exact solver posts through setPosition. Root and bounded read-only Luna
review found no blocker in the seven-file correction/test delta. Ten independent
off-grid solver fixtures/20 assertions and the existing resistance lifecycle pass;
actual CONT/OHM reads0 in both directions, and a generic catalog repair passes
HIGH/LOW customer function. Open/diode/loaded-DC expectations remain independent.
Q15 adds66 directed copper readings and22 malformed reader canaries, with original
admission/work/time bounds unchanged. See final-shop/continuity-review.json.
The optional stale-refresh hypothesis is unproven and does not explain this defect.

The storefront uses the real public category/specification projection for all
cards and filters. It preserves distinct lead fits, source inventory counts,
ephemeral modal/session handles and disabled purchase guards. Root reran syntax
and88 jsdom assertions; real category/search/purchase/focus/power checks are
recorded separately. Previous failed compiled canaries and the missing-method
build failure remain in `final-shop/`; no pre-fix PASS is used for relocated-part
ownership. The launcher correction passed real Windows stale/reuse/foreign
canaries, the actual root CMD startup, and exact child exit propagation; shared
verifier isolation remains unchanged. See final-shop/launcher-recovery.json.


The final bounded read-only session/catalog review found no blocker in exact
replay transport, qualified entropy, generation/session tokens, modal lease
revocation, private projection, or purchased-part identity/compatibility. This
is client Java/GWT ownership, not a server boundary. See
[the review receipt](final-shop/session-review-continuity.json).

Focused JDK8 compilation caught an invalid typed-slot/physical-slot comparison
in the new invariant. The worker corrected the comparison and the fifteen-class
production/controller delta compiled with exit0. This intermediate compile does
not replace final-source build/native/runtime qualification. The failure remains
in [focused evidence](../U04/shop-focused-checks.json).

Root inspected the integrated production, contract, script and documentation
changes, including the new files outside the tracked diff. The review followed
exact replay into generation, pre-publication difficulty assessment, session and
modal leases, public projection, actual catalog acquisition and installation,
partial-write compensation and post-retest completion. It also checked the
numeric wall-clock change and its independent clock vectors against unchanged
electrical/work/deadline limits. `git diff --check` passes. Staged review and
publication have not occurred.

The findings below retain the chronological qualification context of earlier
candidates. References to pending trials/builds are historical; the current
status is the gate ledger and the opening review above.

Findings resolved in this session:

- Four package adapters lacked partial-write compensation. They now use the
  existing declared mutation scope, exact modification-owner guards and typed
  attachment restoration. The independent compiled matrix checks all seven
  adapters; the separate NMOS seed1 case covers short-fault path compensation.
- Retest completion previously ran before ordinary input-restoration analysis
  settled. Completion now checks the exact pending owner, result and session,
  and revokes its bounded watchdog before finishing the challenge.
- Typed composed sessions incorrectly used a leaf Quick Play publication seam.
  Explicit completion and legacy Quick Play selection are now distinct request
  properties.
- A normal legacy verifier flag exposed a developer failure report. A common
  debug guard now protects dispatch, including legacy flags.
- The MEDIUM Shop heading rendered an opaque component identifier. Public
  labels now come from the physical region/component names; opaque acquisition
  IDs remain unchanged. The final three-input delta is recorded separately.

Optional worker evidence has limited scope. The Astra UI worker reviewed and
tested its JavaScript/CSS adapter, then the two-file label correction. The Luna
difficulty investigation reviewed interpretation of reading and action metrics.
The RC investigation was read-only. The acquisition review found the four-adapter
gap; a bounded Luna worker implemented the assigned twelve-file correction.
Its own compile attempt was blocked by its selected JDK/source layout; root's
actual JDK8 builds, native contracts and compiled canaries provide runtime proof.
Those implementation assignments are not whole-candidate reviews. A subsequent
Luna read-only integrated review found no concrete blocker in exact seed/replay
transport, all seven acquisition adapters and compensation, difficulty/session
ownership, public labels, numeric wall clocks and the Quick Play settlement
correction. It did not independently run builds or runtime gates. Root then
added settlement at entry to the verifier's retest helper after inspecting
temporal/NPN status observers. The one-line follow-up review also found no
blocker: those observers can queue input-restoration analysis and the real
customer retest rejects unsettled owners. No build or runtime result is claimed
from either read-only review.

Retained limits: human difficulty calibration is not supplied by mathematical
fixtures or agent-operated inputs. Tight RC work-unit margins and the recorded
concurrent normal-RC/Q15 failure remain visible. Browser locator timeouts are not
successful clicks; the later actual pointer cancellation and exact replay
recovery are recorded separately. Historical A11-D1 and Windows wrapper limits
remain unchanged. A background preview launch was rejected with only “blocked
by policy”; a managed foreground execution session subsequently started the
documented preview and passed its identity check.


The A08 regression exposed an obsolete requirement that the LED catalog must be
unscoped. Its developer-only fixture now accepts the real owned R1/LED
declarations, rejects a detached unscoped catalog and a foreign scoped provider
with exact reasons, and checks that the live owner is unchanged. The bounded
read-only reviewer found no behavioral blocker but missed a local variable name
collision; actual JDK8 compilation caught that issue. The new runtime local was
renamed, and the failing compile is retained. Static review is not compilation
proof; the subsequent native/build/compiled results provide that evidence.


RC phase integration review: root found and corrected an obsolete controller
helper reference, cancellation references dropped before successful cleanup,
extra UI solver frames between phases, source observations refreshed after a
solver call (which could bless intervening changes), and cancellation marked
finished before a restoration failure was propagated. The finished cursor also
checks the exact graph contents and physical mutation receipt. The actual
current-source native/build gates and A10's independent501 RC phase assertions
pass. The focused RC3 repair/admission passes at82units with a1,967ms maximum.
Full Alpha/Q15 and remaining affected qualification are still pending; these
focused results do not establish release acceptance.

Follow-up read-only review found two release blockers after the first-phase gates: staged abort could re-energize an already-OFF candidate during owned cancellation; observation cancellation discarded its retry handle after a failed instrument exit. Root moved final isolation into candidate disposal, retained failed observation cursors and the exclusive private-graph lease, and added exact-graph guards and retry canaries through the real instrument registry. First-phase Alpha38 passed in848,144ms with2ms cleanup but does not qualify these later fixes. Native13 is running; fresh compiled canaries remain pending.

Cleanup follow-up review (read-only leaf): the current cursor/controller strategy and session retry canaries cover failed exit retention, blocked resume/publication, exact graph refusal, lease retention and eventual disposal. No blocker in those fixes was reported. Scope limits: injected strategy deactivation exercises the real instrument controller but does not itself install a measurement overlay; swapping the graph vector does not replace the owner/controller. Existing Task43P measurement and callback routes will provide fresh complementary coverage. The deliberate immediate cleanup retry is documented; the first failure still fails admission. The older synchronous installation failure-marking wrapper remains a lower-priority follow-up, outside these staged/proof retry fixes. No independent runtime testing was performed by the reviewer.

Task43P complementary runtime failed on the explicit-debug route: lane B/F attempted to use a null private workbench. The initial normal installation had a workbench, but private debug installations omitted Task43P from the existing creation predicate. Root added its verifier flag and retained a lane-specific exception wrapper. Standalone wrong-repair, meter and stress helpers passed on the previous cleanup build. Both full-route failures remain preserved. This is a developer-route construction fix; native14 and a final-source build/compiled rerun are required.

Task43P kind6 then reached the native-stamp failure but expected the pre-executor restoration message. The current CircuitSolverExecutor.requireSolvedMatrix contract throws SolverExecutionBoundary.Failure with NUMERICAL_FAILURE and `CircuitJS stopped: Exception in stampCircuit()`. The assertion now requires that exact type/outcome/message, and prints the actual caught error on mismatch. All subsequent overlay/matrix/isolation/queued-power checks remain unchanged. This is an oracle migration, not a production error-contract change. Runtime rerun pending; original failure preserved in task43p-native-stop-failure.json.

Final35b0bbb2 compiled Task43P completed with runtime OBSERVED; maintained Assert-Task43PRuntimeEvidence returned exit0 and zero open blockers, including four overlay failures, seven cleanup-safety cases and actual owner/callback replacement. The malformed contract probe also passed. This closes the complementary runtime gap noted above; it does not certify the historical Windows browser wrapper.

Final visible menu keyboard inspection found a real UI issue despite the earlier Settings/Shop focus passes. Chromium retained getClientRects() boxes for textareas inside the closed replay details element, so reverse wrapping attempted to focus a hidden textarea. The filter now excludes closed disclosure content while keeping its direct summary and all opened contents. The jsdom layout shim now reproduces those real rectangles; four new collapsed/expanded wrap assertions bring the fresh adapter run to49. Java/GWT/electrical inputs remain byte-identical; see menu-focus-input-delta.json. Fresh Browser proof now passes all six collapsed/expanded menu and Resources keyboard checks; the settled focus screenshot was inspected.


Final entry/privacy delta review: a fresh legacy debug button reproduced the
browser crash seen in the existing tab. The pre-existing asynchronous branch
returned its click and admitted the board. The root changed only that button's
scheduling choice; startup/verifier scheduling, selected request and budgets
remain unchanged. Current-source real input returns the first click in289ms
and reaches two sixteen-part boards. Failed cancellation transport/coordinate
attempts remain explicit and are not attributed to the coordinator.

The final read-only session review found an earlier Task43P admission hook that
preceded the common late debug guard. Root reproduced a normal LED3 preparation
failure/private status with debug=false and forced-source parameters. Debug is
now parsed before legacy verifier flags, and those flags are gated at input.
The reviewer inspected the exact one-file delta and found no remaining blocker
in scheduling or debug gating; it did not run independent runtime tests. Root
reviewed the complete CirSim diff and current replay/session consumers. Fresh
normal input now reaches the ticket with no private reports; the explicit-debug
counterpart still activates the invalid-request hook. Native17 and the actual
five-permutation build pass. Full current runtime qualification is still in
progress; previous candidate PASS results retain their original identity.

Final qualification boundary: all current native17/JDK8-GWT/Alpha38/Q15/A10/
Quick Play and affected compiled gates pass, including maintained strict and
combined readers, malformed canaries and forced cleanup. Final visible inputs
pass replay/error recovery/cancellation, full RB15 and MEDIUM acquisition/
physical repair/retest, privacy under all41 verifier flags plus forced Task43P
source parameters, and ordinary Quick Play entries. Eight final screenshots
were inspected. The final audit independently rehashes all1151 consumed inputs
and the maintained1435-file execution tree and checks exact preview ownership;
no reviewed source changed. Prior review scopes and failures above remain
historical, not unresolved final-source gate claims. Staged review is NOT RUN.

Human seed3 EASY/MEDIUM trials remain NOT RUN, and alpha acceptance/publication
remain pending for that roadmap requirement. Agent-operated seed0 repairs do
not establish human difficulty. No independent reviewer ran the final runtime
matrix. The existing A11-D1, browser transport and synchronous installation
failure-marking follow-ups remain outside this accepted technical scope.
