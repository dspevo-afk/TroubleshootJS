# Procedural layouts for every current family; resistor shop/formation repair

Status: QUALIFIED on the isolated publication candidate. Base: 8a807c4c4eaac4979b6d05d6edd22b95710d9ce6. The enclosing commit identifies the publication.

## Implementation and difficulty scope

Every one of the nine current normal board families now reaches real seeded placement and routing. Normal entropy is no longer reduced to curated seed lists, and no fixed full-board layout is used as a normal fallback. Independent named placement/routing streams preserve the full signed root. Fresh roots change relative arrangements and copper routes; exact replay stays exact. The relay no longer receives hand-written coordinates and trace waypoints. Authored developer fixtures are not player fallbacks.

The family set is LED_INDICATOR, DIODE_PROTECTED_INDICATOR, PARALLEL_DUAL_INDICATOR, RC_DELAY, NPN_LOW_SIDE_SWITCH, NMOS_LOW_SIDE_SWITCH, RELAY_OUTPUT, RB15_CONTROL and COMPOSED_CONTROLLED_INDICATOR.

Difficulty remains genuine. The procedural policy supports every available profile, but full diagnostic evidence must still satisfy the requested profile. EASY and MEDIUM are currently enabled; HARD and PSYCHOTIC remain unavailable. This is not a new Medium electrical variant for every simple family, a four-difficulty content expansion, or a difficulty-calibration claim. Normal menu eligibility remains eight Easy families and the composed Medium family. No physics, hypotheses, instruments or difficulty labels were weakened to force admission.

## Resistor purchase, installation and rollback

Direct catalog replacement binds geometry inside the part factory, before acquisition validation. A shop purchase may instead create an explicitly unformed loose catalog axial resistor. Its source inventory remains authoritative. Compatibility queries are pure. The owned installation transaction forms leads to the target; mounted commit requires that exact geometry. Abort restores the same unformed part, inventory, slot and graph. Formed parts retain their shape after removal; originals and other packages do not gain arbitrary reshaping permission.

Compiled A08 passed 1246 assertions, including the new resistor shop/formation scenario and 101 compensated-write checks overall. It exercises powered purchase rejection, isolated purchase, acquisition rollback, formation rollback, original-fault preservation, wrong-value failure and correct-value CircuitJS retest. These are actual controller/solver mutations, not mouse-input installation evidence. A separate normal-player test clicks the actual Shop/Add control and verifies one new loose part on the same isolated board.

## Additional defects corrected

A retained failing Medium seed 17 exposed a synchronous layout step exceeding the unchanged five-second work bound. Controlled composition now routes in job-owned bounded turns before electrical allocation. Hypotheses may copy only immutable physical coordinates tied to the exact request; every electrical/runtime owner and every electrical/repair proof remains fresh. Foreign request identity rejects before assembly. No solver answer or completed proof is cached.

A routed compaction can shorten direct endpoint separation faster than its copper path, exceeding the existing detour limit. The compactor now retains the original legal input when the contraction fails that limit. The matched-input test verifies unchanged original geometry, determinism, all packages/pads, rigid terminal movement and probe dimensions. It does not compare unrelated winners from different routing algorithms or claim minimum possible board area.

CPU profiling identified generic priority-queue overhead in routing. A typed binary heap now uses exactly the existing search-node comparison and sequence tie-breakers. Independent JDK priority-queue parity covers 150,000 mixed/tied operations and final drains (262,586 assertions), and all 144 before/after structural records preserve their discrete geometry. Search costs, expansion caps, route ordering and the five-second benchmark ceiling are unchanged. The earlier 5,163 ms and 5,128 ms A10 performance failures remain recorded; they were not relabeled passes.

## Fresh evidence

The isolated candidate passes nineteen affected Java suites with 1,173,968 reported assertions; the Quick Play suite reports no assertion count. JDK8/GWT OBF compiles and links all five permutations. The full repository matrix and historical population readers were NOT RUN.

The structural corpus contains 144 of 144 accepted candidates: sixteen roots per family, split into declared development/holdout cohorts. Each eight-root cohort has eight distinct full geometries and at least four relative-order macro layouts. Both relay cohorts have eight macro layouts. Translation, margins, values and fault IDs are excluded from macro identity. Same-root and all admitted-fault physical replays pass, as does staged/synchronous Medium 17 parity. Native assertions compare complete strings; JSON hash codes are summaries, not uniqueness proofs.

The normal browser driver passes 18 of 18 actual New Board launches, two saved exact replays, relay root4 in the new epoch, retained-owner cancellation, obsolete-epoch rejection, answer privacy and a real Shop purchase. The final Medium 17 launch took 7.46 seconds in this run. That is an observation, not a universal latency promise.

Compiled A08/E03/A10 and an explicitly failing A08 canary pass their expected outcomes. Each driver closes its browser/server and reports zero owned process survivors; native class scratch cleanup passes. The final clean Edge permutation is F63F0B949660B0B120FDF246ECE5A01D. Full screenshots and source/compiled hashes are retained. Two earlier same-root relay overviews were inspected at thumbnail resolution for arrangement differences, board fit and meter separation; no new full-resolution art/readability acceptance is claimed.

## Retained failures and limitations

The pre-correction normal run passed 17 of 18 and rejected Medium 17 on the work-unit bound. It is not counted as a passing population; the final driver repeats both roots for all nine families. Scratch also retains obsolete fixed-footprint/whitelist assertions, an initial test requesting a non-admitted connector hypothesis, missing sealing in the first staged patch, packing harness setup failures, and the actual detour regression. A later normal harness sampled only 0% before a successful 0.16-second completion; it now requires intermediate progress for preparations lasting at least one second and records unsampled fast completion explicitly. No failed sample was deleted to inflate success rates; production guards remain intact.

This modest sample does not establish arbitrary-circuit or every-seed success. Larger boards, new packages/layers, arbitrary topology generation, advanced difficulty content and every hypothetical family/profile combination are not qualified. Existing 90-second/640-unit/five-second bounds and P09 physical/access limits are unchanged. Timing and RC-rich boards remain slower than simple boards. Prior RB15 development24/holdout48 rates belong to their older source and are not relabeled current.

Current identity is tsj-alpha/3, interpretation11, admission3 and layout13. Old replay epochs reject rather than silently create a different PCB. New example: tsj-alpha/3/EASY/RELAY_OUTPUT/4.

The scoped publication excludes unrelated FPS, tray, renderer and visual work. The owner's game was separately rebuilt with that pending work retained, and targeted final relay/Medium/replay/shop checks passed. The isolated candidate received the full18-case run. Main staging was preserved during implementation; only consumed scoped paths may be reconciled after publication. See REPRODUCE.md.

The final local build and targeted four-case player/replay/shop/cancellation check are recorded in owner-integration.json. The direct compiler process returned zero, source hashes stayed unchanged, and all owned browser processes closed. An earlier wrapper incorrectly expected PowerShell LASTEXITCODE to be set after a successful script call; the direct-process receipt replaces that ambiguous wrapper result. No game code change was needed for that collection issue.
