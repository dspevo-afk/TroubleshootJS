# Superseded: visual pass completed

The earlier paused handoff below is historical. Follow [VISUAL_PASS_CHECKPOINT.md](VISUAL_PASS_CHECKPOINT.md) and [final33 evidence](task-evidence/visual-productization/final33/README.md). The visual/service correction is qualified in the completion commit containing this update; P05 is unstarted. Do not execute the older remaining-work list.

---

# Post-REL-A checkpoint — pause at 1% remaining usage

**Status: unfinished, uncommitted, not published.** Updated 2026-09-13 local time
(2026-09-14 UTC). The user asked for a durable checkpoint at1% remaining usage;
the live main weekly allowance now has1% remaining. Pause here and resume from
this file. No automatic continuation or notification was scheduled.

## First actions on resume

1. Confirm branch/HEAD/status and preserve every recovered edit and untracked
   file. Read this checkpoint, the top of CODEX_TASK_REPORT.md, ROADMAP.md and the
   new ownership section in ARCHITECTURE.md. Do not reread the historical log.
2. The last compiled candidate is **GWT15**. Three subsequent verifier edits are
   **not built or tested**. Review those changes, then run a new actual JDK8/GWT
   build before using the preview to qualify them.
3. Recheck the E03 regression first, then the changed architecture/Task43P
   verifiers. Continue remaining acceptance below. No heavy gate is running.
4. Keep tracking usage. Do not mark this task complete or publish an unresolved
   blocker. P05 remains outside this task and unstarted.

Workspace: TroubleshootJS. Branch: `codex/task43p-final-recovery`.
HEAD/base: `f41e80f6227ffd04c0092ff01207bdc8e392c2ed`.
Upstream: `origin/codex/task43p-final-recovery`.
Origin: `https://github.com/dspevo-afk/TroubleshootJS.git`.
Root was the only writer; no agents are active. Nothing is staged. No commit,
push or completion email occurred for this work. Preserve pre-existing untracked
`tests/contracts/__pycache__/` and recovered raw visual evidence.

## Authorized scope

Resume the recovered visual/productization pass and small developer RENDER-0
proof. Add subtle realistic wood grain. The user additionally requires **every
actual part on every playable board** to have supported removal/replacement,
independent of selected fault, health, diagnosis or correct repair. Healthy
removal and wrong compatible replacements/eligible positions must change the
real CircuitJS graph, with original identity/provenance retained in the tray.
Keep existing owner, power, meter, residual-energy and transaction guards. Keep
supported polarity reversal. Do not give arbitrary developer-only packages fake
operations. Stop before P05.

User AGENTS authorizes the eventual explicit-path commit, normal origin push,
remote SHA verification and one established Gmail completion. No more permission
is required for that sequence. Finish validation first.

## What is implemented

- `ServiceableBoardConstruction` completes physical service ownership before
  admission using package/specification/bindings/runtime, without fault data.
  All nine playable families and71 seed0 representative positions have real
  providers. Supporting resistors, LEDs, diodes, transistors and capacitors reuse
  typed providers; connectors/headers/fuses have genuine service owners.
- Connector solder leads and detachable cable contacts have actual WireElm
  backing and transaction rollback. Fuses retain ProtectionFuseElm behavior.
  Stable parts, terminals, board endpoints, mounts, inventory and original
  provenance survive replacement. Purchased parts can cross eligible positions.
- Parallel R1/R2 both have unconditional providers and secondary-open paths.
  Fault admission supports either resistor. Diagnostic route identity includes
  its owner; the maintained route corpus now has22 routes.
- Normal READY checks no longer demand the original private fault symptom after
  legal player edits. Admission still proves healthy/faulted profiles; customer
  retest judges actual resulting behavior.
- Stored-energy readiness follows current installed/loose capacitors, including
  charged wrong-value supporting replacements and their real terminals.
- Visual work: consolidated top controls, wood grain fixed to bench space,
  masked copper and metal pads, polished packages, adjacent metal tray sharing
  the camera, guarded physical dragging, matching bent lifted-lead paint/probe
  tips, half-visible pan limits, centered TO-92 bodies and body context menus.
- Developer RENDER-0 attaches a tiny SVG backend to the same immutable physical
  scene and electrical owner. It is a bounded attachment proof, not a completed
  alternate product renderer.

Larger tray inventories exposed RC solver timeouts. The implementation preserves
all equations, timesteps,90s generation limits,5s temporal limits and work budgets.
Analysis caches wire neighbors and voltage recipients in their original order;
exact ideal wires bypass inherited empty callbacks. Dynamic matrix writes check
finite inputs/results. Crout factorization accumulates the same terms in the
same per-entry order and skips zero contributions across disconnected islands.
Original pivots/substitution remain. `LuFactorizationChecks` retains the previous
algorithm as an independent JVM/GWT oracle for exact factors, pivots and solutions.

## Candidate and source boundary

`docs/task-evidence/visual-productization/candidate.json` describes **GWT15**.
`final-inputs.json` freezes779 source/script/test/UI inputs for15.

| Identity | GWT15 value |
|---|---|
| Source | `9ae7b37132d6ff10464e7c329710c4a62a461d4965d01fc3068cc321a13981c4` |
| Scripts | `6fce407515899eccb49d52fb38f770bf2400abf85924006295237077c2e4c61a` |
| Web output | `7913a729c96f4a0f805d1152114b5f8f303cf069848bac362d7d81fdf37c6a20` |
| Execution | `69b1024cb02a4a2c6f591b63d4ba4151061e3118de8a677a8c9bf8a91497b9d7` |
| Execution files | 1456 |

`candidate-build14.json` and `inputs-build14.json` preserve the completed full
corpus candidate. The sole14→15 source/input delta was
`A08FreshOwnerAliasVerifier.java` (audited). Its deliberately foreign operational
owner is now rejected during construction, before installation. The fixture now
asserts that exact earlier rejection plus unchanged live state. The separate
altered-LED install-time ownership negative remains. GWT15 A08 passed afterward.

**Three new verifier edits after GWT15 are unbuilt and untested:**

1. `E03RelayMutationChecks.java`: replaces the obsolete assertion that healthy
   D1 flyback removal is unavailable. It now powers off/discharges, removes the
   real healthy diode, checks disconnected solder elements and exact tray
   identity/provenance, reinstalls the same diode, then repeats HIGH/LOW retest.
2. `ArchitectureDeveloperVerifier.java`: provenance records origin, not
   serviceability. Checks discovery through declared runtime service providers
   and exact provider identity; unsupported parts still must not expose fake
   operations. The separate755-assertion coverage gate requires providers at
   every playable position.
3. `Task43PPhysicalTruthDeveloperVerifier.java`: service connectors now have
   separable component-side endpoints. Checks each solder binding's board and
   component endpoint identities; genuinely fixed foundation parts retain the
   original same-endpoint assertion.

These are developer proof updates, not further changes to player behavior. Their
compile/runtime results are NOT RUN. Current source differs from the preview's
startup source fingerprint. Never label that preview as built from these edits.

## Passing evidence and limits

Evidence directory: `docs/task-evidence/visual-productization/`.

| Gate | Candidate and actual result |
|---|---|
| JDK8/GWT build | GWT15 PASS,5 permutations,69.882s compile/1.320s link,exit0 |
| Full native | GWT15 PASS,35 Java suites, independent seed/value/role oracles, report474, cleanup,exit0 |
| A08 lifecycle | GWT15 PASS1103 assertions,90 compensated writes,5 fresh-install failures,45174ms,cleanup PASS; strict reader and empty-report negative PASS |
| Full Alpha | GWT14 PASS38 cases,356 service positions,9835 assertions,310 mutation checks,9 provider categories |
| Alpha detail | GWT14:400 acquisitions,379 stale-provider callbacks,357 negatives;1146895ms operation/3ms cleanup,exact owner restored |
| Alpha strict reader | GWT14 full and forced reports PASS;38 malformed canaries rejected |
| RC cases | GWT14 all3 repaired retests PASS; admissions51376/51427/51456ms,max unit1980ms |
| A07 | GWT14 PASS24867 pure/73 runtime/59 model assertions; original Crout comparisons, real matrix NaN/overflow, strict/negative reader checks |
| Service | GWT14 PASS61: both fault owners, healthy removal→zero current, exact reinstall→baseline, wrong100k retained with actual low current |
| RENDER-0 | GWT14 PASS65/six negatives, exact identity/solver/meter/repair/cleanup |
| Forced cleanup | GWT14 Alpha/service/RENDER-0 deliberately FAIL at intended points and restore exact owners |
| UI adapter | Current unchanged UI inputs PASS108 plus node syntax; DOM shims do not prove real input |
| Stored energy | Older GWT9 PASS including charged wrong1uF C2 removal and active-meter rejection; current confirmation pending |

GWT14 Parallel current: healthy branch about4.832mA or9.750mA → removal0 → same
part restored to baseline → wrong100k about0.03755mA. Seed0's two resistor spans
are physically different, so cross-position fit correctly rejects. Composed
fixtures prove installation in eligible wrong positions and wrong-value stress.

Preserve failures: earlier RC admission/temporal deadlines; the old wire oracle
used mismatched transient/display times and now compares from identical inputs;
GWT13 focused RC passed but full retest hit5s at144715 accepted steps. Receipts
remain. GWT14 A08 failure was its newly earlier construction rejection
(`a08-build14-failure.json`), fixed and passed15. GWT15 E03 still expected a fixed
flyback (`e03-build15-failure.json`); its new test is unbuilt. Do not suppress,
relabel or broaden budgets to hide failures.

Full final-candidate corpus/companion evidence remains to qualify after the new
verifier edits. Audit every consumed input before reusing prior evidence; a
passing result on older inputs is not automatically a final-source PASS. The
historical Windows CDP wrapper is unqualified. No independent agent review ran.

## Actual final-build player input already checked

Browser tab1 uses normal GWT15 Parallel0, without developer flags.

- Accepted the actual ticket. R1 and healthy R2 expose identical Lift1/Lift2/
  Remove menus at power OFF (`final-ui-menus.txt`).
- Lifted healthy R2 lead1 and placed red on the actual lifted tip, black on its
  other terminal:680 Ohm (`final-healthy-lifted-measurement.jpg`).
- Reconnected that lead, dragged R2 into the real tray and powered ON: both LEDs
  dark, original R2 visible in tray (`final-healthy-removed-powered.jpg`). Actual
  customer retest reported it did not pass.
- Powered OFF and dragged that same tray resistor back to R2. UI reported
  `Part installed.` **This is the current state**. A powered restoration check,
  wrong-value replacement and correct final repair are still needed here.

Inspected current wood/bench overview and healthy menu screenshots also exist:
`final-bench-overview.jpg`, `final-healthy-resistor-menu.jpg`. Older build6 images
are not current proof. Keep2–5 useful final screenshots and distinguish normal
visible input from developer-controller evidence.

## Remaining acceptance, in order

1. Review the three unbuilt verifier edits; build with:
   `scripts/build.ps1 -JavaHome .tools/jdk8-download/jdk8u502-b07`.
   Recheck E03, architecture and Task43P affected routes before a long corpus.
2. Freeze new source/output/input identities; repeat applicable final native and
   compiled gates. Only one expensive full build/matrix at a time.
   Full native command (no `-Suite`):
   `scripts/verify-current-contracts.ps1 -JavaHome .tools/jdk8-download/jdk8u502-b07`.
   Default JAVA_HOME points to Android JBR and is unsuitable.
3. Outstanding compiled companions: E03, Task41(22 routes), Task49, Q15(all11
   seeds/66 copper readings), A10, stored-energy and applicable viewport/meter/
   lifecycle/render checks. Current-source service/RENDER0 and deliberate cleanup
   negatives also need an explicit evidence decision. A07 and Alpha last full
   passes are14. Preserve useful independent oracles; don't weaken assertions.
4. Maintained readers: `alpha_evidence_contract.py --report <alpha-report.json>
   --negative <alpha-forced.json>` requires38 cases/38 malformed rejections.
   `scripts/verify-q15-evidence.ps1 -EvidenceRoot <folder>` expects named current
   E03/A08/Task41/Task49/Q15/A10 reports and checks22 Task41 routes. Run each
   relevant strict reader and actual failure cleanup canary.
5. Finish normal visible healthy restoration, wrong100k compatible replacement
   and failed retest, then correct R1 medium330Ω repair and successful retest.
   Check a larger board, pan/flip/tray/toolbar and ordinary UI/ARIA/title/tooltips
   for selected-fault leakage. Inspect/save final screenshots.
6. Root integrated diff review; update architecture/roadmap/evidence README and
   compact task report; `git diff --check`. Stage intended paths explicitly,
   inspect cached diff/check, commit, normal origin push, verify exact remoteSHA.
   Do not blindly stage recovered raw logs or personal absolute paths.
7. After verified publication, established Gmail completion to the AGENTS
   recipient with exactSHA, branch, scope/checks/limits and nextP05. If unavailable,
   report published work and notification failure separately. Do not duplicate
   the prior base release's message `1a09ca4dda7fc336`.

## Resources and browser recovery

**No build/native/compiled gate is currently running.** Build15/session25616 and
native15/session93230 finished exit0. The sole intentionally retained task preview
is ready for continuation:

- URL `http://127.0.0.1:64326`; run`visual-service-final15`.
- Exec session70986; PID22972; UTC start ticks639249476410079743.
- Nonce`dea213734895400bb9e844ffcfc53180`.
- Exact executable/PID/start/port in `preview15-owner.json`; startup fingerprints
  in `preview15-identity.json` under task scratch.
- Verify PID/start/executable/command/port/runId before stopping. Owned prior
  previews64320–64325 were verified/stopped. Leave uncertain earlier resources.

Task scratch under OS temp:
`TroubleshootJS-visual-resume-dea213734895400bb9e844ffcfc53180`.
Raw logs/receipts: build15.log, native15.log/native15.txt and earlier failures.
Do not commit personal filesystem paths or uncontrolled logs.

Built-in Browser tool is initialized in persistent Node with `browser`, `pilot`
(tab1), `serviceTab`(tab2), `evidenceFs`, `evidenceDir`. If its kernel reset, use the
Browser skill's bootstrap, select the same browser, list tabs and rebind them.
Do not navigate while a gate is running. During the19-minute Alpha14 run, busy
periods caused CDP reads to time out; reconnecting recovered the same progressing
page. The separate normal tab stayed responsive. Actual final report PASS was
saved. A screenshot of the busy developer page is diagnostic, not player evidence.

Tab1: `http://127.0.0.1:64326/circuitjs.html?replay=tsj-alpha/1/EASY/PARALLEL_DUAL_INDICATOR/0`.
R2 reinstalled; power OFF; R1 original fault remains. Meter mode exited.
Tab2: same64326 app with `tsjDebug=true&tsjChallenge=led&seed=3&tsjVerifyE03=true`;
it has the old flyback-expectation FAIL. Preserve the saved failure; no active gate.

Observed1200×1272 UI coordinates: R1(288,644),R2(278,865),R2pads(194,859)/(362,859),
lifted lead1(224,835),tray first body(1085,539). Confirm a fresh screenshot before
reuse. CUA right-click is button3; button2 is middle. Drag takes a `path` array.
Buttons: `Board Power: ON/OFF`, `OHM`, `Component`, `Run customer retest`.
Shop searchbox: `Search components`; narrow100k fits R2, medium330Ω fits R1.
A power click can complete before the screenshot repaints; read current DOM after
settlement before repeating it. Do not mistake stale paint for a rejected click.

Read gate reports from DOM attributes with the Browser's read-only evaluation:
`data-tsj-alpha-report`, `data-tsj-service-report`, `data-tsj-render0-report`,
`data-tsj-a07-report`, `data-tsj-a08-report`, `data-tsj-e03-report`,
`data-tsj-q15-report`, `data-tsj-task41-evidence`, `data-tsj-task49-report`,
`data-tsj-a10-report`, plus generic`data-tsj-verification`. Save raw terminal reports.
Use log-level browser logs too; GWT failures can appear there rather than errors.
Some flags take priority over others; verify each requested report exists.
E03 is explicitly suppressed if Q15 is selected. Do not substitute an external
Playwright/CDP driver for the built-in Browser.
