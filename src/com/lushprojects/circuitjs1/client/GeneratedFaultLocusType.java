package com.lushprojects.circuitjs1.client;

/** Stable physical boundary for a solver-backed generated fault. */
enum GeneratedFaultLocusType {
    COMPONENT_INTERNAL,
    TERMINAL_ATTACHMENT,
    CONNECTOR_CONTACT,
    TRACE_SEGMENT
}
